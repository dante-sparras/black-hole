import { defineConfig } from 'vite'

/**
 * GitHub Pages project site: https://dante-sparras.github.io/black-hole/
 * Asset URLs must be rooted under /black-hole/ (not /).
 */
export default defineConfig({
  base: '/black-hole/',
})
